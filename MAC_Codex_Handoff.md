# Mac Codex Handoff

Use this document as the first thing to read when opening this repo on Mac.

For the current Windows-side “拆解项目” context, read `WINDOWS_PROJECT_CONTEXT_HANDOFF.md` first. That file contains the concrete repo URLs, Windows paths, current status, important files, known issues, and exact next actions for Mac Codex.

## What this repo is for

This repo exists to transfer context between:

- the Windows-side Codex session
- the Mac-side Codex session

The user wants two things to be visible from Mac:

1. the earlier GitHub "拆解项目"
2. the Windows-side pet prototype called "咕咕嘎嘎"

For a usable handoff, also fill in `PROJECT_CONTEXT_TEMPLATE.md` and `PET_PROTOTYPE_NOTES.md`.
If the visual or motion assets are missing, check `ASSET_INVENTORY.md`.

## Known context

- The user previously uploaded a decomposition / analysis project to GitHub
- The user also has a Windows-side pet prototype with the working name "咕咕嘎嘎"
- The user wants Mac Codex to understand both without re-explaining everything from scratch

## What Mac Codex should do first

1. Read this file
2. Read `WINDOWS_PROJECT_CONTEXT_HANDOFF.md`
3. Read `README.md`
4. Read `PROJECT_CONTEXT_TEMPLATE.md`
5. Read `PET_PROTOTYPE_NOTES.md`
6. Read `ASSET_INVENTORY.md`
7. Inspect the rest of the repository
8. Look for the original implementation files or references
9. If the actual project content is missing, ask for the source repo or the missing files rather than guessing

## What is not known yet

Do not assume any of these until the user provides them or the repo contains them:

- the exact scope of the拆解项目
- the actual architecture of the pet prototype
- the final feature list
- the intended visual style
- the platform target

## Suggested next entries

If the user sends more detail, add short sections for:

- project goal
- current branch of work
- file map
- prototype behavior rules
- known issues
- next actions
