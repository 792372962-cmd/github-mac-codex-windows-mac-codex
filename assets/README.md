# Assets

Drop the real prototype assets here.

Suggested layout:

- `images/` for screenshots and stills
- `frames/` for animation frame sequences
- `video/` for screen recordings or demo clips
- `source/` for exported prototype source files

If these folders are empty, the repo cannot reproduce the pet prototype visually.

